package elements;

import org.openqa.selenium.WebDriver;

public class Button  extends BaseElement {

    public Button(String locator, String nameElement, WebDriver driver) {
        super(locator, nameElement, driver);
    }
}
