package elements;

import org.openqa.selenium.WebDriver;

public class Checkbox extends BaseElement {

    public Checkbox(String locator, String nameElement, WebDriver driver) {
        super(locator, nameElement, driver);
    }
}
